# Build outputs
dist/
build/
.svelte-kit/
storybook-static/
coverage/

# Dependencies
node_modules/

# Generated files
__screenshots__/
*.tsbuildinfo

# Runtime-injected / vendored assets (mirrors the eslint ignore) — served
# as-is, not formatted as app modules
src/assets/

# Vendored READ.html reader build and Paged.js polyfill (third-party, served as-is)
public/read/
public/paged.polyfill.js
public/sr-preview/
# Vendored foliate-js renderer for the READ.html device preview (third-party, served as-is)
public/foliate/
# Vendored Chromium XML tree viewer for the Source view (third-party, kept
# close to upstream for diffability)
public/xml-tree-viewer/
patches/

# Vendored minified libraries (e.g. axe-core for the accessibility check)
*.min.js

# Vendored cuelume build, served dev-only beside the agent bridge module
# (third-party, kept close to upstream for diffability)
src/lib/agent-bridge/cuelume.js

# Workspace plugins are separate packages with their own formatting config
plugins/

# Extensions catalog: vendored 3rd-party libs + example scripts, served as-is
extensions/

# Config files that should keep their original formatting
*.config.js
*.config.ts
vite.config.ts
vitest.config.ts

# Tooling / agent config (not project source)
.claude/

# Local Python virtualenv (untracked) — not project source
.venv/

# Book sources for the in-app manuals: prose is deliberately one line per
# paragraph (reviewed in Obsidian) — never reflow
docs/

# Temporary files
.DS_Store
*.tmp
*.log

# Generated icon subset (scripts/generate-icons.js) — byte-stable output, not formatted
src/lib/icons/generated/
